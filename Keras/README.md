This implementation was based on the official site of [keras](https://keras.io/examples/vision/image_classification_with_vision_transformer/).  
The reason it was rejected is because it uses functions to implement the transformer, which use many parameters.  
As a result, the time for each epoch exceeds the normal time and to solve the problem requires high resources.
